{
  "name": "jcip-house-hostinger-deploy",
  "version": "1.2.1",
  "private": true,
  "description": "Deploy wrapper for Hostinger. The API application lives in backend-node.",
  "scripts": {
    "postinstall": "npm install --prefix backend-node --omit=dev",
    "start": "npm start --prefix backend-node"
  },
  "engines": {
    "node": ">=20"
  }
}
